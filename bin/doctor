#!/bin/sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if [ "${MODEL_ROUTER_TARGET:-codex}" != codex ]; then
  echo "Only the Codex target has a public doctor operation." >&2
  exit 2
fi
exec node "$repo_dir/src/doctor.mjs" "$@"
